<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>KMUTT Class Schedule</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600&display=swap"
      rel="stylesheet"
    />
  </head>
  <body class="bg-gray-100 font-inter">
    <div class="container mx-auto px-4 py-8 max-w-xl">
      <div class="bg-white shadow-lg rounded-xl overflow-hidden">
        <div class="bg-blue-600 text-white p-5">
          <h1 class="text-2xl font-bold text-center">Today's Class Schedule</h1>
        </div>

        <div class="p-6">
          <% if (schedule && schedule.length > 0) { %> <%
          schedule.forEach((item, index) => { %>
          <div
            class="flex items-center mb-4 pb-4 <%= index < schedule.length - 1 ? 'border-b border-gray-200' : '' %>"
          >
            <div
              class="bg-blue-100 text-blue-800 px-3 py-1 rounded-full mr-4 font-semibold"
            >
              <%= item.start %>
            </div>
            <div class="flex-grow">
              <h3 class="text-lg font-medium text-gray-800">
                <%= item.subject %>
              </h3>
            </div>
          </div>
          <% }); %> <% } else { %>
          <div class="text-center text-gray-500">
            No classes scheduled for today.
          </div>
          <% } %>
        </div>

        <div class="bg-gray-50 p-4 text-center text-sm text-gray-600">
          Last updated: <%= new Date().toLocaleString() %>
        </div>
      </div>
    </div>
  </body>
</html>
